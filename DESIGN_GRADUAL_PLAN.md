# 🎨 ПЛАН ПОСТЕПЕННЫХ УЛУЧШЕНИЙ ДИЗАЙНА

**Дата:** 4 ноября 2025  
**Цель:** Красивый дизайн из Figma, но **ПОСТЕПЕННО!**  
**Статус:** ✅ Сайт откачен на рабочую версию

---

## ✅ Текущая ситуация

- **Сайт работает:** https://beltehferm.netlify.app/
- **Версия:** 18fb8f2 (стабильная, до изменений)
- **Figma интегрирована:** Token и File ID есть
- **Дизайн:** https://www.figma.com/make/qoAaKOIY4f24zEBKZot0Aw/AGRO-App

---

## 🎯 Принцип работы

### ❌ НЕ ДЕЛАТЬ:
- Менять всё сразу
- Удалять старый код
- Ломать структуру
- Push без проверки

### ✅ ДЕЛАТЬ:
- **По 1 элементу** за раз
- **Тестировать** локально
- **Показывать** вам перед push
- **Сохранять** старый код как fallback

---

## 📅 ПЛАН (4 недели)

### НЕДЕЛЯ 1: Цвета и шрифты (фундамент)

#### День 1: Цветовая палитра
**Что делаем:**
1. Добавить Figma цвета в `variables.css`
2. **НЕ МЕНЯТЬ** старые цвета
3. Добавить **новые переменные** рядом

**Файл:** `frontend/css/variables.css`
```css
/* СТАРЫЕ ЦВЕТА - НЕ ТРОГАТЬ */
:root {
  --brand-primary: #2a9d4e;
  --brand-accent: #ff5b1a;
}

/* НОВЫЕ ЦВЕТА ИЗ FIGMA */
:root {
  --figma-primary: #00A859;
  --figma-secondary: #F5F5F5;
  --figma-accent: #FF6B35;
  --figma-dark: #1A1A1A;
  --figma-light: #FAFAFA;
}
```

**Результат:** Старый дизайн работает, новые цвета готовы

---

#### День 2: Типографика
**Что делаем:**
1. Добавить Figma шрифты (Inter, Apercu)
2. **НЕ МЕНЯТЬ** старые шрифты сразу
3. Создать классы `.figma-text`

**Файл:** `frontend/index.html` (в `<head>`)
```html
<!-- Figma шрифты -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

**Файл:** `frontend/css/variables.css`
```css
/* Новая типографика */
:root {
  --figma-font-primary: 'Inter', sans-serif;
  --figma-font-mono: 'Courier New', monospace;
}

.figma-text {
  font-family: var(--figma-font-primary);
}
```

**Результат:** Шрифты загружены, но старый дизайн не изменился

---

#### День 3-4: Кнопки (первый видимый элемент)
**Что делаем:**
1. Создать **НОВЫЕ** классы кнопок `.btn-figma`
2. **НЕ МЕНЯТЬ** старые `.btn`
3. Применить к 1-2 кнопкам для теста

**Файл:** `frontend/css/components.css`
```css
/* НОВЫЕ кнопки из Figma */
.btn-figma {
  font-family: var(--figma-font-primary);
  background: var(--figma-primary);
  color: white;
  padding: 14px 28px;
  border-radius: 12px;
  border: none;
  font-weight: 600;
  font-size: 16px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 12px rgba(0, 168, 89, 0.2);
}

.btn-figma:hover {
  background: #008F4D;
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(0, 168, 89, 0.3);
}
```

**Применить к 1 кнопке:**
```html
<!-- В index.html - заменить ОДНУ кнопку -->
<a href="catalog.html" class="btn-figma">Смотреть каталог</a>
```

**Тест:**
- Одна кнопка новая
- Остальные старые
- Если нравится → меняем остальные
- Если нет → откатываем эту одну

---

### НЕДЕЛЯ 2: Карточки товаров

#### День 5-7: Дизайн карточек
**Что делаем:**
1. Создать `.product-card-figma`
2. Применить к 1 товару в каталоге
3. Остальные - старые

**Файл:** `frontend/css/product-cards.css`
```css
/* Новые карточки из Figma */
.product-card-figma {
  background: white;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 2px 16px rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
}

.product-card-figma:hover {
  transform: translateY(-8px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
}

.product-card-figma__image {
  width: 100%;
  height: 240px;
  object-fit: cover;
}

.product-card-figma__content {
  padding: 20px;
}

.product-card-figma__title {
  font-family: var(--figma-font-primary);
  font-size: 18px;
  font-weight: 600;
  color: var(--figma-dark);
  margin-bottom: 12px;
}

.product-card-figma__price {
  font-size: 24px;
  font-weight: 700;
  color: var(--figma-primary);
}
```

**Применить:**
```html
<!-- В catalog.html - заменить ПЕРВУЮ карточку -->
<div class="product-card-figma">
  <img src="..." class="product-card-figma__image">
  <div class="product-card-figma__content">
    <h3 class="product-card-figma__title">DONGFENG DF-244</h3>
    <div class="product-card-figma__price">285 000 ₽</div>
    <a href="#" class="btn-figma">Подробнее</a>
  </div>
</div>
```

**Тест:**
- 1 карточка новая
- 10 карточек старые
- Сравниваем
- Если ОК → меняем остальные постепенно

---

### НЕДЕЛЯ 3: Header и Navigation

#### День 8-10: Header
**Что делаем:**
1. Создать `.header-figma`
2. Добавить переключатель между версиями
3. **НЕ УДАЛЯТЬ** старый header

**Подход:**
```html
<!-- Переключатель версий (временный, для теста) -->
<style>
  .header-old { display: none; }
  .header-figma { display: block; }
</style>

<!-- Старый header (скрыт, но не удален) -->
<header class="header-old modern-header">
  <!-- Старый код -->
</header>

<!-- Новый header -->
<header class="header-figma">
  <!-- Новый дизайн из Figma -->
</header>
```

**Если не нравится:**
```css
.header-old { display: block; }
.header-figma { display: none; }
```
→ Мгновенный откат!

---

### НЕДЕЛЯ 4: Доработка и полировка

#### День 11-14: Главная страница целиком
**Что делаем:**
1. Hero секция
2. Статистика
3. Преимущества
4. Футер

**Принцип:**
- По 1 секции в день
- Тестируем каждую
- Старый код не удаляем

---

## 🔧 Технический подход

### Feature Toggle (переключатель)

**Создать:** `frontend/js/design-toggle.js`
```javascript
const DESIGN_CONFIG = {
  // Включить новый дизайн
  useNewDesign: false, // false = старый, true = новый
  
  // Какие элементы обновлены
  components: {
    buttons: false,
    cards: false,
    header: false,
    footer: false
  }
};

// Применить дизайн
if (DESIGN_CONFIG.useNewDesign) {
  document.body.classList.add('figma-design');
} else {
  document.body.classList.add('classic-design');
}
```

**CSS:**
```css
/* Старый дизайн (по умолчанию) */
.classic-design .btn {
  /* старые стили */
}

/* Новый дизайн (когда включено) */
.figma-design .btn {
  /* новые стили из Figma */
}
```

**Преимущества:**
- Мгновенный откат (просто `useNewDesign = false`)
- Можно включать по компонентам
- A/B тестирование

---

## 📋 Чек-лист перед каждым изменением

- [ ] Создал бэкап: `git tag backup-design-$(date +%Y%m%d)`
- [ ] Протестировал локально
- [ ] Сделал скриншот ДО
- [ ] Сделал скриншот ПОСЛЕ
- [ ] Показал ВАМ перед push
- [ ] Не удалил старый код
- [ ] Добавил переключатель (если большое изменение)
- [ ] Проверил на мобильных

---

## 🎨 Первые шаги (СЕГОДНЯ)

### Шаг 1: Добавить Figma цвета (5 минут)

```bash
# Отредактировать frontend/css/variables.css
# Добавить новые цвета РЯДОМ со старыми
# Закоммитить
git add frontend/css/variables.css
git commit -m "🎨 DESIGN: Add Figma color variables (no visual changes)"
git push origin main
```

**Результат:** Сайт выглядит ТАК ЖЕ, но новые цвета готовы

---

### Шаг 2: Заменить 1 кнопку (10 минут)

```bash
# Создать .btn-figma в components.css
# Заменить ОДНУ кнопку на главной
# Тест локально
npm start
# Если ОК → push
```

**Результат:** 1 кнопка новая, остальные старые

---

### Шаг 3: Показать ВАМ (1 минута)

Скриншот → вы говорите ОК или НЕТ → если ОК продолжаем

---

## 🚀 Ожидаемые результаты

### После недели 1:
- ✅ Новые цвета и шрифты добавлены
- ✅ 2-3 кнопки обновлены
- ✅ Сайт работает стабильно
- ✅ Старый код на месте

### После недели 2:
- ✅ Карточки товаров красивые
- ✅ Каталог выглядит современно
- ✅ Можно откатить за 1 минуту

### После недели 3:
- ✅ Header обновлен
- ✅ Navigation современная
- ✅ Полностью адаптивно

### После недели 4:
- ✅ Весь сайт в новом дизайне
- ✅ Полировка и баг-фиксы
- ✅ Production ready

---

## 💡 Мои обязательства

**Я буду:**
1. ✅ Работать по 1 элементу
2. ✅ Показывать ВАМ каждое изменение
3. ✅ Не удалять старый код
4. ✅ Создавать бэкапы
5. ✅ Тестировать локально

**Я НЕ буду:**
1. ❌ Менять всё сразу
2. ❌ Ломать структуру
3. ❌ Push без вашего одобрения
4. ❌ Удалять рабочий код

---

## 📞 Следующий шаг

**СПРОСИТЬ ВАШЕГО ОДОБРЕНИЯ:**

Начать с **Шага 1** (добавить цвета)?
- Займет 5 минут
- Сайт не изменится
- Только подготовка

**ИЛИ**

Подождать и сделать когда будете готовы?

---

**Автор:** Claude Code  
**Принцип:** Красиво, но ПОСТЕПЕННО! 🎨
