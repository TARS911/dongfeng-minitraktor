# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ - ДЕЛАЙТЕ САМИ

## 🎯 ЧТО ВАМ НУЖНО СДЕЛАТЬ

Я подготовил всё необходимое, но вам нужно выполнить 3 простых шага:

---

## 📝 ШАГ 1: ПРИМЕНИТЬ SQL МИГРАЦИИ (5 минут)

### Что делать:

1. **Откройте Supabase Dashboard**
   ```
   https://supabase.com/dashboard
   ```

2. **Выберите ваш проект**

3. **Откройте SQL Editor** (в левом меню)

4. **Выполните первую миграцию:**
   - Откройте файл: `docs/migrations/fix-equipment-category.sql`
   - Скопируйте весь код
   - Вставьте в SQL Editor
   - Нажмите **RUN** (или Ctrl+Enter)
   - Должно выполниться без ошибок

5. **Выполните вторую миграцию:**
   - Откройте файл: `docs/migrations/audit-trail.sql`
   - Скопируйте весь код
   - Вставьте в SQL Editor
   - Нажмите **RUN**
   - Дождитесь выполнения (~30 секунд)

6. **Настройте RLS политики:**
   ```sql
   -- Скопируйте и выполните в SQL Editor:
   
   DROP POLICY IF EXISTS "Allow public insert on categories" ON categories;
   CREATE POLICY "Allow public insert on categories"
     ON categories FOR INSERT
     WITH CHECK (true);
   
   DROP POLICY IF EXISTS "Allow public update on categories" ON categories;
   CREATE POLICY "Allow public update on categories"
     ON categories FOR UPDATE
     USING (true);
   
   DROP POLICY IF EXISTS "Allow public delete on categories" ON categories;
   CREATE POLICY "Allow public delete on categories"
     ON categories FOR DELETE
     USING (true);
   
   DROP POLICY IF EXISTS "Allow public insert on products" ON products;
   CREATE POLICY "Allow public insert on products"
     ON products FOR INSERT
     WITH CHECK (true);
   
   DROP POLICY IF EXISTS "Allow public update on products" ON products;
   CREATE POLICY "Allow public update on products"
     ON products FOR UPDATE
     USING (true);
   ```

7. **Проверьте результат:**
   ```sql
   -- Выполните в SQL Editor:
   SELECT id, name, slug, description 
   FROM categories 
   WHERE slug = 'equipment';
   
   -- Должно показать: "Коммунальная техника"
   
   SELECT COUNT(*) FROM audit_log;
   -- Должно вернуть 0 (таблица создана)
   ```

✅ **ШАГ 1 ВЫПОЛНЕН!**

---

## 🌐 ШАГ 2: ДЕПЛОЙ НА NETLIFY (3 минуты)

У вас есть **2 варианта**:

### ВАРИАНТ A: Автоматический (если настроен GitHub)

**Netlify автоматически деплоит при push на GitHub.**

1. Проверьте что изменения запушены:
   ```bash
   cd /home/ibm/dongfeng-minitraktor
   git status
   # Должно быть: "Your branch is up to date with 'origin/main'"
   ```

2. Откройте Netlify Dashboard:
   ```
   https://app.netlify.com
   ```

3. Найдите ваш сайт в списке

4. Проверьте вкладку **Deploys**
   - Должен быть запущен новый деплой
   - Дождитесь статуса "Published" (~3-5 минут)

5. **Добавьте переменные окружения** (если ещё не добавлены):
   - Site settings → Environment variables
   - Добавьте:
     - `NEXT_PUBLIC_SUPABASE_URL` = ваш Supabase URL
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = ваш anon key

6. Если переменные добавили сейчас, нажмите **Trigger deploy**

---

### ВАРИАНТ B: Ручной деплой (через CLI)

1. **Установите Netlify CLI** (если ещё не установлен):
   ```bash
   npm install -g netlify-cli
   ```

2. **Запустите автоматический скрипт**:
   ```bash
   cd /home/ibm/dongfeng-minitraktor
   ./deploy.sh
   ```

   Скрипт автоматически:
   - Проверит окружение
   - Установит зависимости
   - Соберёт проект
   - Задеплоит на Netlify

3. **Авторизуйтесь в Netlify** (когда попросит)

4. **Дождитесь завершения**

✅ **ШАГ 2 ВЫПОЛНЕН!**

---

## 🧪 ШАГ 3: ПРОВЕРКА (5 минут)

### 1. Откройте ваш сайт
```
https://ваш-сайт.netlify.app
```

Должна загрузиться главная страница без ошибок.

---

### 2. Проверьте админ-панель
```
https://ваш-сайт.netlify.app/admin/categories
```

Должна открыться страница управления категориями.

**Попробуйте создать категорию:**
1. Нажмите "+ Добавить категорию"
2. Заполните форму:
   - Название: `Тестовая категория`
   - Slug: `test-category`
   - Описание: `Тест после деплоя`
3. Нажмите "Создать"

Если создалась - значит всё работает! ✅

---

### 3. Проверьте API
```bash
# Проверка списка категорий
curl https://ваш-сайт.netlify.app/api/categories

# Должен вернуть JSON с категориями
```

---

### 4. Проверьте Audit Log (в Supabase)
В Supabase SQL Editor выполните:
```sql
SELECT * FROM get_recent_changes(10);
```

Должны увидеть запись о созданной тестовой категории.

---

## ✅ ГОТОВО!

Если все 3 шага прошли успешно - **ваш сайт в production!** 🎉

---

## 📊 ЧТО ТЕПЕРЬ ДОСТУПНО

### Админ-панель:
- `/admin/categories` - управление категориями

### API:
- `GET/POST /api/categories` - работа с категориями
- `GET/PUT/DELETE /api/categories/:id` - CRUD операции
- `GET /api/products?page=1&search=...` - поиск товаров
- `POST /api/import/products` - массовый импорт
- `POST /api/import/bitrix` - интеграция с 1С

### Возможности:
- ✅ Rate Limiting
- ✅ Pagination
- ✅ Поиск
- ✅ Audit Trail
- ✅ Security Headers

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Build fails на Netlify?
1. Проверьте переменные окружения:
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY
2. Проверьте логи сборки в Netlify
3. Попробуйте локально: `npm run build`

### API возвращает ошибки?
1. Проверьте что SQL миграции применены
2. Проверьте RLS политики в Supabase
3. Проверьте логи в Netlify Functions

### Админ-панель не открывается?
1. Проверьте что RLS политики разрешают запись
2. Откройте DevTools → Console
3. Проверьте Network вкладку

---

## 📞 НУЖНА ПОМОЩЬ?

Смотрите подробную документацию:
- `QUICK_DEPLOY.md` - быстрый деплой
- `IMPLEMENTATION_REPORT.md` - полный отчёт
- `docs/migrations/apply-migrations.md` - детали миграций

---

## 🎯 БЫСТРЫЙ ЧЕКЛИСТ

Используйте этот чеклист для деплоя:

```
☐ Открыл Supabase Dashboard
☐ Выполнил fix-equipment-category.sql
☐ Выполнил audit-trail.sql
☐ Настроил RLS политики
☐ Проверил что миграции применились
☐ Открыл Netlify Dashboard
☐ Проверил что деплой запущен
☐ Добавил environment variables (если нужно)
☐ Дождался завершения деплоя
☐ Открыл сайт - работает
☐ Открыл /admin/categories - работает
☐ Создал тестовую категорию - работает
☐ Проверил API - работает
☐ Проверил audit_log в Supabase - работает
```

Если все галочки стоят - **ГОТОВО!** 🚀

---

**Время выполнения: ~15 минут**

**Сложность: Лёгкая** (просто следуйте инструкции)

**Результат: Production-ready сайт** ✅
