# 🚀 Прямой деплой на Netlify (рекомендуется)

## ❌ Проблема
GitHub Actions деплоит неправильно - старый сайт не обновляется.

## ✅ Решение
Использовать прямую интеграцию Netlify с GitHub.

---

## 📋 Пошаговая инструкция

### Шаг 1: Удалить старый сайт (если есть)

1. Зайдите на https://app.netlify.com
2. Найдите ваш старый сайт
3. Site settings → General → Danger zone → Delete this site

### Шаг 2: Создать новый сайт с интеграцией GitHub

1. Откройте https://app.netlify.com
2. Нажмите **Add new site** → **Import an existing project**
3. Выберите **Deploy with GitHub**
4. Авторизуйте Netlify (если нужно)
5. Найдите репозиторий: **TARS911/dongfeng-minitraktor**
6. Нажмите на него

### Шаг 3: Настроить Build Settings

Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

**Build command:**
```
cd frontend && npm install && npm run build
```

**Publish directory:**
```
frontend/.next
```

**Environment variables (обязательно добавить):**
```
NEXT_PUBLIC_SUPABASE_URL = https://wbfhvcmvkyjsjvqkbxpz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndiZmh2Y212a3lqc2p2cWtieHB6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjEyNzg1MzksImV4cCI6MjA3Njg1NDUzOX0.5yHwVSIkbhDnnUKrPSe6uTCW-ImZYrczI-8nRQB0fHY
NODE_VERSION = 20
```

### Шаг 4: Deploy!

1. Нажмите **Deploy site**
2. Дождитесь завершения (3-5 минут)
3. Получите URL типа `https://ваш-сайт.netlify.app`

---

## 🔄 Автоматический деплой

Теперь при каждом `git push` в `main`:
- Netlify автоматически начнет сборку
- Соберет проект
- Задеплоит на сайт
- **Без GitHub Actions!**

---

## ⚙️ Что уже настроено в коде

1. **`netlify.toml`** - конфигурация сборки
   - Build command
   - Publish directory
   - Node version
   - Plugin Next.js

2. **GitHub Actions отключен** - не мешает Netlify

3. **Next.js настроен для SSR** - не используется static export

---

## 🐛 Устранение ошибок

### Ошибка: "Build failed"

**Проверьте логи в Netlify Deploy logs:**

1. Откройте ваш сайт в Netlify
2. Перейдите в **Deploys**
3. Кликните на последний deploy
4. Смотрите логи

**Частые проблемы:**

#### 1. Missing environment variables
```
Error: Missing Supabase environment variables
```
**Решение:** Добавьте переменные окружения в Site settings → Environment variables

#### 2. Build command failed
```
npm ERR! missing script: build
```
**Решение:** Проверьте что build command правильный:
```
cd frontend && npm install && npm run build
```

#### 3. Plugin not found
```
Error: Cannot find plugin @netlify/plugin-nextjs
```
**Решение:** Netlify автоматически установит plugin из `netlify.toml`

### Ошибка: "Supabase connection failed"

**Проверьте что переменные окружения добавлены:**
1. Site settings → Environment variables
2. Должны быть:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### Старый сайт все еще показывается

**Очистите кэш:**
1. В Netlify → Site settings → Build & deploy
2. Нажмите **Clear cache and retry deploy**
3. Или откройте сайт в режиме инкогнито (Ctrl+Shift+N)

---

## 📊 Как проверить что деплой работает

### 1. Проверить последний деплой

1. Откройте https://app.netlify.com
2. Выберите ваш сайт
3. Смотрите статус последнего деплоя:
   - 🟢 **Published** - успешно
   - 🟡 **Building** - в процессе
   - 🔴 **Failed** - ошибка

### 2. Проверить что код обновился

Сделайте тестовый коммит:
```bash
git commit --allow-empty -m "🧪 Test deploy"
git push
```

Через 3-5 минут сайт должен обновиться.

### 3. Проверить что Supabase работает

1. Откройте ваш сайт
2. Откройте DevTools (F12) → Console
3. Не должно быть ошибок типа "Missing Supabase"
4. На главной странице должны появиться товары из базы

---

## 🎯 Финальный чеклист

- [ ] Старый сайт удален (если был неправильный)
- [ ] Создан новый сайт через GitHub integration
- [ ] Build settings настроены правильно
- [ ] Environment variables добавлены
- [ ] Первый деплой успешен (статус Published)
- [ ] Сайт открывается и работает
- [ ] Supabase подключение работает
- [ ] Автодеплой работает при git push

---

## 📞 Полезные ссылки

| Что | Где |
|-----|-----|
| Netlify Dashboard | https://app.netlify.com |
| Настроить новый сайт | https://app.netlify.com/start |
| Site settings | https://app.netlify.com/sites/[ваш-сайт]/settings |
| Deploy logs | https://app.netlify.com/sites/[ваш-сайт]/deploys |
| Environment variables | https://app.netlify.com/sites/[ваш-сайт]/settings/env |
| Netlify Docs (Next.js) | https://docs.netlify.com/integrations/frameworks/next-js/ |

---

## 💡 Почему прямая интеграция лучше

| GitHub Actions | Netlify Direct |
|---------------|----------------|
| ❌ Сложная настройка | ✅ Автоматически |
| ❌ Нужны токены и секреты | ✅ OAuth интеграция |
| ❌ Медленнее (2 сервиса) | ✅ Быстрее |
| ❌ Больше точек отказа | ✅ Надежнее |
| ❌ Сложнее дебажить | ✅ Понятные логи |

---

## 🆘 Нужна помощь?

1. Проверьте логи деплоя в Netlify
2. Проверьте что все переменные окружения добавлены
3. Попробуйте Clear cache and retry deploy
4. Убедитесь что `netlify.toml` в корне репозитория
5. Проверьте что в frontend есть `package.json` с командой `build`
