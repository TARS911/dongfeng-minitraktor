# Быстрый старт: Импорт запчастей в Supabase

## 🚀 Готово к импорту

✅ **2836 запчастей** конвертированы в PostgreSQL SQL
✅ **Схема базы данных** готова
✅ **Миграция** подготовлена

## 📋 Что делать дальше (3 шага)

### 1️⃣ Создать таблицы (1 минута)

Откройте **Supabase SQL Editor** и выполните:

```
backend/database/supabase-migration-parts.sql
```

### 2️⃣ Импортировать данные (5-10 минут)

Импортируйте файл частями или целиком:

```
backend/database/supabase-parts-data.sql
```

**Размер:** 2.8 MB (2836 INSERT statements)

### 3️⃣ Проверить результат

```sql
SELECT COUNT(*) FROM parts;  -- Должно быть: 2836
SELECT * FROM parts LIMIT 10;
```

## 📁 Файлы миграции

```
backend/database/
├── supabase-migration-parts.sql    ← ШАГ 1: Схема
├── supabase-parts-data.sql         ← ШАГ 2: Данные (2836 товаров)
├── README-PARTS-MIGRATION.md       ← Полная документация
└── QUICKSTART-PARTS.md             ← Этот файл
```

## 🔍 Быстрая проверка после импорта

```sql
-- Топ-10 самых дорогих запчастей
SELECT name, price, manufacturer
FROM parts
WHERE price IS NOT NULL
ORDER BY price DESC
LIMIT 10;

-- Количество по производителям
SELECT manufacturer, COUNT(*) as count
FROM parts
WHERE manufacturer IS NOT NULL
GROUP BY manufacturer
ORDER BY count DESC;

-- Поиск по названию
SELECT * FROM search_parts('гидравлика') LIMIT 10;
```

## 📚 Структура данных

Каждая запчасть содержит:
- ✅ Название (транслитерированный slug)
- ✅ Цена в рублях
- ✅ Производитель (Dongfeng, Jinma, Uralets и т.д.)
- ✅ Изображение (URL Agrodom)
- ✅ Ссылка на источник
- ✅ Артикул (SKU, если есть)

## ⚠️ Важно

**Файл данных большой (2.8 MB)** - если Supabase SQL Editor выдаёт ошибку "timeout", разбейте файл на части:

```bash
# Разбить на части по 500 строк
split -l 500 backend/database/supabase-parts-data.sql parts-chunk-

# Импортируйте каждую часть отдельно
```

Или используйте PostgreSQL клиент напрямую.

## 💡 Полезные ссылки

- 📖 Полная документация: `README-PARTS-MIGRATION.md`
- 🔧 Скрипт конвертации: `scripts/convert-agrodom-to-postgres.py`
- 📊 Исходные данные: `parsed_data/agrodom/products-clean.json`

---

**Готово!** После импорта у вас будет **2836 запчастей** в Supabase PostgreSQL 🎉
