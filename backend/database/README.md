# Миграция базы данных на Supabase

Этот каталог содержит скрипты для миграции данных из SQLite в Supabase (PostgreSQL).

## Содержание

- `config.js` - Общая конфигурация и утилиты (credentials, валидация, резервное копирование)
- `supabase-schema.sql` - SQL-схема для Supabase с RLS политиками
- `migrate-to-supabase.js` - Основной скрипт миграции данных из SQLite
- `import-to-supabase.js` - Импорт тестовых данных (категории + 3 товара)
- `import-remaining-products.js` - Импорт дополнительных 8 товаров
- `export-sqlite-data.js` - Экспорт всех данных из SQLite в JSON

## Требования

- Node.js 18+
- npm или yarn
- Доступ к Supabase проекту

## Установка

### 1. Установка зависимостей

```bash
npm install @supabase/supabase-js better-sqlite3 dotenv
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `backend/database/`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваши credentials:

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SQLITE_DB_PATH=./dongfeng.db
```

**Где найти credentials:**
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Settings → API
4. Скопируйте `Project URL` и `anon/public key`

## Порядок миграции

### Шаг 1: Создание схемы в Supabase

Выполните SQL-скрипт в Supabase SQL Editor:

1. Откройте Supabase Dashboard → SQL Editor
2. Нажмите "New query"
3. Скопируйте содержимое файла `supabase-schema.sql`
4. Выполните запрос

Это создаст:
- 4 таблицы (categories, products, contacts, delivery_requests)
- Индексы для оптимизации
- RLS политики безопасности
- Триггеры и функции
- Таблицу версий схемы

### Шаг 2: Миграция данных из SQLite

**ВАЖНО:** Этот скрипт автоматически создаст резервную копию SQLite!

```bash
node migrate-to-supabase.js
```

Что делает скрипт:
1. Создает резервную копию `dongfeng_backup_YYYY-MM-DD.db`
2. Проверяет существующие данные в Supabase
3. Запрашивает подтверждение, если БД не пуста
4. Мигрирует все данные с валидацией
5. Использует `upsert` для обработки дубликатов

Лог будет показывать прогресс:
```
💾 Создание резервной копии SQLite...
✅ Резервная копия создана: dongfeng_backup_2025-11-14.db

🔍 Проверка существующих данных в Supabase...

📦 Миграция категорий...
[1/3] Миграция категории: Минитрактора
✅ Минитрактора (ID: 1)
...

📊 Категории: 3 успешно, 0 ошибок
```

### Шаг 3 (Опционально): Импорт тестовых данных

Если вы хотите начать с чистой БД и тестовых данных:

```bash
# Импорт категорий + 3 товара
node import-to-supabase.js

# Добавить ещё 8 товаров
node import-remaining-products.js
```

## Безопасность

### Credentials
- **НИКОГДА** не коммитьте файл `.env` в Git
- `.env` уже добавлен в `.gitignore`
- Используйте `.env.example` как шаблон

### Ротация API ключей

Если credentials случайно попали в Git:

1. Откройте Supabase Dashboard → Settings → API
2. Нажмите "Reset" рядом с ключом
3. Обновите `.env` с новым ключом
4. Удалите credentials из Git history:
   ```bash
   git filter-branch --force --index-filter \
   'git rm --cached --ignore-unmatch backend/database/.env' \
   --prune-empty --tag-name-filter cat -- --all
   ```

### Row Level Security (RLS)

Схема включает RLS политики:
- **Категории и товары**: Публичное чтение, изменение только для авторизованных
- **Контакты и доставка**: Анонимное создание, чтение только для авторизованных

## Валидация данных

Все скрипты используют встроенную валидацию из `config.js`:

```javascript
validators.validateProduct(product);  // Проверка обязательных полей
validators.validateCategory(category);
validators.validateContact(contact);
validators.validateDeliveryRequest(request);
```

Валидация проверяет:
- Наличие обязательных полей
- Корректность типов данных
- Положительные значения цен и мощности
- Валидность slug'ов

## Утилиты

### Экспорт всех данных из SQLite в JSON

```bash
node export-sqlite-data.js
```

Создаст файл `sqlite-export.json` со всеми данными.

### Проверка данных в Supabase

```javascript
import { checkExistingData } from './config.js';

const data = await checkExistingData();
console.log(data); // { categories: 3, products: 11, isEmpty: false }
```

### Создание резервной копии вручную

```javascript
import { createBackup, config } from './config.js';

await createBackup(config.sqliteDbPath);
// Создаст dongfeng_backup_2025-11-14T10-30-45.db
```

## Обработка ошибок

### Ошибка: "Отсутствуют обязательные переменные окружения"

```
❌ ОШИБКА: Отсутствуют обязательные переменные окружения!
```

**Решение**: Создайте файл `.env` с корректными credentials.

### Ошибка: "Категория не найдена"

```
❌ Категория minitractory не найдена!
```

**Решение**: Сначала запустите `import-to-supabase.js` для создания категорий.

### Ошибка валидации

```
❌ Ошибка валидации: Некорректная цена для Товар: -100
```

**Решение**: Исправьте данные в исходном файле перед импортом.

### Дубликаты

Скрипты используют `upsert` с `onConflict: 'slug'`, поэтому:
- При повторном запуске данные **обновятся**, а не создадутся заново
- Дубликаты по `slug` автоматически разрешаются

## Версионирование схемы

Таблица `schema_version` отслеживает версии миграций:

```sql
SELECT * FROM schema_version ORDER BY applied_at DESC;
```

```
version | description                              | applied_at
--------|------------------------------------------|-----------
1.0.0   | Начальная схема: категории, товары...   | 2025-11-14
```

## Полезные SQL запросы

### Проверка данных после миграции

```sql
-- Количество записей
SELECT 
  (SELECT COUNT(*) FROM categories) as categories_count,
  (SELECT COUNT(*) FROM products) as products_count,
  (SELECT COUNT(*) FROM contacts) as contacts_count,
  (SELECT COUNT(*) FROM delivery_requests) as delivery_count;

-- Товары по категориям
SELECT c.name, COUNT(p.id) as product_count
FROM categories c
LEFT JOIN products p ON c.id = p.category_id
GROUP BY c.name;

-- Последние добавленные товары
SELECT name, model, price, created_at
FROM products
ORDER BY created_at DESC
LIMIT 10;
```

### Очистка БД (если нужно начать заново)

```sql
-- ВНИМАНИЕ: Удалит все данные!
TRUNCATE TABLE delivery_requests CASCADE;
TRUNCATE TABLE contacts CASCADE;
TRUNCATE TABLE products CASCADE;
TRUNCATE TABLE categories CASCADE;
```

## Troubleshooting

### Скрипт зависает на подтверждении

Если `migrate-to-supabase.js` ждёт ввода `yes/no`:
- Введите `yes` или `y` для продолжения
- Введите `no` или `n` для отмены
- Можно изменить код, чтобы пропустить подтверждение

### Проблемы с правами доступа SQLite

```
Error: SQLITE_CANTOPEN: unable to open database file
```

**Решение**: Проверьте путь к `dongfeng.db` в `.env`:
```env
SQLITE_DB_PATH=/полный/путь/к/dongfeng.db
```

### Проблемы с Supabase подключением

```
Error: Invalid API key
```

**Решение**: 
1. Проверьте правильность `SUPABASE_URL` и `SUPABASE_ANON_KEY`
2. Убедитесь, что в ключе нет лишних пробелов
3. Попробуйте пересоздать ключ в Supabase Dashboard

## Дополнительная информация

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase JS Client](https://supabase.com/docs/reference/javascript/introduction)
- [PostgreSQL JSONB](https://www.postgresql.org/docs/current/datatype-json.html)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

## Поддержка

При возникновении проблем:
1. Проверьте логи выполнения скрипта
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Node.js (должна быть 18+)
4. Проверьте Supabase Dashboard на наличие ошибок

## Лицензия

MIT
