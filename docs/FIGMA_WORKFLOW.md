# Figma Design System Workflow

Руководство по ежедневной работе с интеграцией Figma и кода для проекта DONGFENG.

## Обзор интеграции

Система обеспечивает двустороннюю синхронизацию между:
- **Figma Variables** ⟷ **CSS Custom Properties** (`variables.css`)
- **Figma Components** ⟷ **HTML/CSS код** компонентов
- **Figma Styles** ⟷ **CSS классы** и токены

## Workflow для дизайнеров

### 1. Обновление дизайн-токенов в Figma

Когда вы изменяете токены (цвета, spacing, typography) в Figma:

1. Внесите изменения в Figma Variables или Styles
2. Сохраните изменения
3. Уведомите команду разработки через Slack/Discord
4. Разработчик запустит синхронизацию: `npm run figma:pull`

### 2. Создание нового компонента

При создании нового компонента в Figma:

1. Создайте Component в Figma со всеми вариантами
2. Используйте существующие Variables для цветов и размеров
3. Добавьте описание компонента в properties
4. Дайте имя компоненту по конвенции: `Component/Variant/State`
   - Пример: `Button/Primary/Default`
5. Сообщите разработчикам File ID компонента
6. Разработчик сгенерирует код: `npm run figma:components:generate`

### 3. Обновление существующего компонента

При изменении компонента:

1. Внесите изменения в master component
2. Убедитесь, что изменения отражены во всех вариантах
3. Обновите версию в описании (если используете)
4. Разработчик пересоздаст компонент или внесёт изменения вручную

## Workflow для разработчиков

### 1. Изменение CSS токенов

Когда вы обновляете `frontend/css/variables.css`:

```bash
# После изменения variables.css

# Вариант A: Загрузить изменения в Figma
npm run figma:push

# Вариант B: Двусторонняя синхронизация (разрешит конфликты)
npm run figma:sync
```

### 2. Извлечение токенов из Figma

Когда дизайнер обновил Figma:

```bash
# Извлечь все токены из Figma в CSS
npm run figma:pull

# Проверить различия перед применением
npm run figma:diff

# Создать бэкап перед синхронизацией
npm run figma:backup
```

### 3. Генерация компонента из Figma

Для создания HTML/CSS кода из Figma компонента:

```bash
# Посмотреть все доступные компоненты
npm run figma:components:list

# Сгенерировать код для конкретного компонента
npm run figma:components:generate
# Следуйте интерактивным промптам
```

Сгенерированный код появится в `frontend/components/`.

### 4. Работа с ветками Git

Рекомендуемый процесс при работе с Figma синхронизацией:

```bash
# 1. Создать feature branch
git checkout -b feature/update-design-tokens

# 2. Синхронизировать токены
npm run figma:sync

# 3. Проверить изменения
git diff frontend/css/variables.css

# 4. Закоммитить
git add frontend/css/variables.css design-tokens/
git commit -m "🎨 Sync design tokens from Figma"

# 5. Создать PR
git push origin feature/update-design-tokens
```

## Разрешение конфликтов

При запуске `npm run figma:sync` могут возникнуть конфликты, если токен изменён и в Figma, и в CSS.

Система предложит выбрать источник истины:

```
⚠ Конфликт: --brand-primary
  Figma: #2a9d4e
  CSS:   #2ea44f
  
Выберите действие:
  1) Использовать значение из Figma
  2) Использовать значение из CSS
  3) Пропустить (оставить как есть)
```

### Автоматическое разрешение

В `figma.config.js` можно настроить:

```javascript
conflictResolution: {
  defaultSource: 'figma', // всегда использовать Figma
  // или
  defaultSource: 'css',   // всегда использовать CSS
  // или
  defaultSource: 'ask',   // спрашивать каждый раз (по умолчанию)
}
```

## Валидация токенов

Перед коммитом проверьте валидность токенов:

```bash
npm run figma:validate
```

Проверки включают:
- ✓ Контрастность цветов (WCAG AA/AAA)
- ✓ Дубликаты значений
- ✓ Корректность naming convention
- ✓ Отсутствующие зависимости (если токен ссылается на другой)

## CI/CD интеграция

### Автоматическая проверка в PR

При создании Pull Request с изменениями в `variables.css`:

1. GitHub Actions автоматически запускает `figma:diff`
2. В PR добавляется комментарий с различиями между Figma и CSS
3. Если различия критичные, PR блокируется для review

### Автоматическая синхронизация

Настроена ежедневная синхронизация (2:00 UTC):

1. GitHub Actions запускает `figma:pull`
2. Если есть изменения, создаётся автоматический PR
3. Team lead проверяет и мерджит

## Best Practices

### Для дизайнеров

1. **Используйте Variables**: Вместо hardcoded значений используйте Variables
2. **Консистентное именование**: Следуйте конвенции `category/subcategory/name`
3. **Документируйте изменения**: Добавляйте описания к Variables
4. **Версионирование**: Используйте Figma Versions для major changes
5. **Коммуницируйте**: Предупреждайте команду о breaking changes

### Для разработчиков

1. **Регулярная синхронизация**: Запускайте `figma:test` раз в день
2. **Бэкапы**: Всегда создавайте бэкап перед major sync
3. **Code review**: Проверяйте сгенерированный код перед использованием
4. **Тестирование**: Проверяйте визуальные изменения после синхронизации
5. **Не редактируйте вручную**: Не изменяйте auto-generated файлы

## Troubleshooting

### Синхронизация долго выполняется

- Проверьте скорость интернета
- Figma API может быть медленным при больших файлах
- Используйте `depth` параметр для ограничения глубины

### Токены не применяются

- Очистите browser cache
- Перезапустите dev server
- Проверьте, что CSS файл импортирован

### Сгенерированный код некорректный

- AI-enhanced генерация экспериментальная
- Используйте как starting point
- Доработайте вручную для production

## Примеры использования

### Пример 1: Обновление цветовой палитры

```bash
# Дизайнер обновил brand colors в Figma

# 1. Разработчик создаёт ветку
git checkout -b update/brand-colors

# 2. Извлекает изменения
npm run figma:pull

# 3. Проверяет diff
git diff frontend/css/variables.css

# 4. Тестирует локально
npm run dev

# 5. Коммитит и создаёт PR
git commit -am "Update brand colors from Figma"
git push
```

### Пример 2: Создание нового компонента

```bash
# Дизайнер создал новый компонент Button/CTA в Figma

# 1. Получить список компонентов
npm run figma:components:list

# 2. Сгенерировать код
npm run figma:components:generate
# Выбрать: Button/CTA

# 3. Проверить сгенерированный код
cat frontend/components/button-cta/button-cta.html
cat frontend/components/button-cta/button-cta.css

# 4. Интегрировать в проект
# ... доработать при необходимости ...

# 5. Закоммитить
git add frontend/components/button-cta/
git commit -m "Add CTA button component from Figma"
```

## FAQ

**Q: Как часто нужно синхронизировать?**
A: Зависит от интенсивности изменений. Рекомендуется 1-2 раза в неделю или по мере необходимости.

**Q: Что делать, если Figma API недоступен?**
A: Используйте локальные бэкапы из `design-tokens/backups/`. Синхронизация будет доступна после восстановления API.

**Q: Можно ли отменить синхронизацию?**
A: Да, используйте бэкапы: `cp design-tokens/backups/variables.css.TIMESTAMP.backup frontend/css/variables.css`

**Q: Поддерживаются ли Figma Plugins?**
A: Пока нет. Интеграция использует только REST API. Plugin API может быть добавлен в будущем.

## Полезные ссылки

- [Figma REST API](https://www.figma.com/developers/api)
- [CSS Custom Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/--*)
- [Design Tokens](https://designtokens.org/)
- [WCAG Contrast Guidelines](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html)
