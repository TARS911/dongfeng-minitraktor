# Настройка Figma Integration

Пошаговое руководство по настройке интеграции Figma Design System с проектом DONGFENG.

## Быстрый старт

```bash
# 1. Установка зависимостей (если ещё не установлены)
npm install

# 2. Запуск мастера настройки
npm run figma:setup

# 3. Проверка подключения
npm run figma:test
```

## Детальная настройка

### 1. Создание Figma Access Token

1. Откройте Figma → Settings (⚙️)
2. Перейдите в Account → Personal Access Tokens
3. Нажмите "Generate new token"
4. Дайте имя токену: "DONGFENG Design System"
5. Выберите scopes (минимум):
   - `file_content:read` - чтение файлов
   - `file_metadata:read` - метаданные
   - `library_content:read` - библиотеки компонентов
6. Скопируйте токен (показывается только один раз!)

### 2. Создание Figma файла дизайн-системы

1. Создайте новый файл в Figma: "DONGFENG Design System"
2. Организуйте страницы:
   - 🎨 **Design Tokens** - цвета, типографика, spacing
   - 🧩 **Components** - кнопки, карточки, формы
   - 📐 **Layouts** - хедер, футер, сетки
   - 🚜 **Product Cards** - карточки тракторов

3. Настройте Variables (Figma 2024+):
   - Создайте коллекции для colors, spacing, typography
   - Используйте существующие значения из `frontend/css/variables.css`

4. Скопируйте File ID из URL:
   ```
   https://www.figma.com/file/FILE_ID_HERE/DONGFENG-Design-System
   ```

### 3. Настройка переменных окружения

Добавьте в `.env` (корень проекта):

```env
FIGMA_ACCESS_TOKEN=figd_ваш_токен_здесь
FIGMA_FILE_ID=ваш_file_id_здесь
```

### 4. Проверка подключения

```bash
npm run figma:test
```

Вы должны увидеть:
```
✓ Токен валиден и имеет доступ к API
✓ Файл найден: "DONGFENG Design System"
✅ Все проверки пройдены успешно!
```

## Основные команды

| Команда | Описание |
|---------|----------|
| `npm run figma:setup` | Интерактивная настройка |
| `npm run figma:test` | Проверка подключения |
| `npm run figma:pull` | Figma → CSS (извлечение токенов) |
| `npm run figma:push` | CSS → Figma (загрузка токенов) |
| `npm run figma:sync` | Двусторонняя синхронизация |
| `npm run figma:components:list` | Список компонентов |
| `npm run figma:components:generate` | Генерация кода |

## Структура Figma файла

Рекомендуемая структура для максимальной совместимости:

```
DONGFENG Design System/
├── 🎨 Design Tokens
│   ├── Colors
│   │   ├── Brand (primary, accent, secondary)
│   │   ├── Neutral (grays, black, white)
│   │   └── Semantic (success, warning, error)
│   ├── Typography
│   │   ├── Font Families
│   │   ├── Font Sizes
│   │   └── Font Weights
│   ├── Spacing (xs, sm, md, lg, xl...)
│   ├── Shadows
│   └── Border Radius
│
├── 🧩 Components
│   ├── Buttons (primary, secondary, sizes)
│   ├── Cards (product-card, feature-card)
│   ├── Forms (input, textarea, select)
│   └── Modals
│
├── 📐 Layouts
│   ├── Header (desktop, mobile)
│   ├── Footer
│   └── Grid System
│
└── 🚜 Product Cards
    ├── Card Default
    ├── Card Hover
    └── Card Mobile
```

## Troubleshooting

### Ошибка: "Invalid token"

- Проверьте, что токен скопирован полностью
- Убедитесь, что токен не истёк
- Создайте новый токен с правильными scopes

### Ошибка: "File not found"

- Проверьте File ID в URL
- Убедитесь, что у токена есть доступ к файлу
- Если файл в Team, добавьте `FIGMA_TEAM_ID`

### Нет доступа к Variables API

- Variables API доступен только в Figma Professional
- Альтернатива: используйте Styles вместо Variables
- Обновите Figma до последней версии

## Следующие шаги

После успешной настройки:

1. Прочитайте [FIGMA_WORKFLOW.md](./FIGMA_WORKFLOW.md) - workflow для команды
2. Запустите первую синхронизацию: `npm run figma:sync`
3. Изучите [FIGMA_API.md](./FIGMA_API.md) - API документация

## Полезные ссылки

- [Figma API Documentation](https://www.figma.com/developers/api)
- [Figma Variables Guide](https://help.figma.com/hc/en-us/articles/15339657135383)
- [Design Tokens Format](https://design-tokens.github.io/community-group/format/)
