# ▲ Деплой на Vercel - Полностью БЕСПЛАТНО!

## Почему Vercel с Supabase - идеальное решение?

✅ **У вас уже есть Supabase PostgreSQL** - значит Vercel подходит идеально!
✅ **Полностью бесплатно навсегда** - без лимитов на кредиты
✅ **Супер быстрый** - edge network по всему миру
✅ **Автодеплой** из GitHub

---

## Шаг 1: Регистрация на Vercel

1. Перейдите на: **https://vercel.com**
2. Нажмите **"Sign Up"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите email

---

## Шаг 2: Подготовка кода

### Ваш код УЖЕ готов! ✅

Вы используете:
- ✅ Supabase PostgreSQL (не SQLite)
- ✅ API routes через Fastify
- ✅ Конфиг `vercel.json` создан

---

## Шаг 3: Создание проекта на Vercel

### Через веб-интерфейс:

1. **В Dashboard Vercel нажмите "Add New..."**
2. **Выберите "Project"**
3. **Import Git Repository:**
   - Выберите ваш GitHub репозиторий: `dongfeng-minitraktor`
   - Нажмите **"Import"**

4. **Configure Project:**
   ```
   Framework Preset:     Other
   Root Directory:       ./  (оставить как есть)
   Build Command:        (оставить пустым)
   Output Directory:     (оставить пустым)
   Install Command:      npm install
   ```

5. **Environment Variables** - добавьте:
   ```env
   SUPABASE_URL=https://wbfhvcmvkyjsjvqkbxpz.supabase.co
   SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   NODE_ENV=production
   ```

6. **Нажмите "Deploy"**

---

## Шаг 4: Проверка деплоя

Vercel автоматически:
1. Клонирует ваш репозиторий
2. Установит зависимости (`npm install`)
3. Задеплоит backend
4. Даст вам URL: `https://dongfeng-minitraktor.vercel.app`

**Время деплоя: 2-3 минуты**

---

## Шаг 5: Тестирование API

После деплоя проверьте:

```bash
# Health check
curl https://dongfeng-minitraktor.vercel.app/api/health

# Получить все товары
curl https://dongfeng-minitraktor.vercel.app/api/products

# Получить категории  
curl https://dongfeng-minitraktor.vercel.app/api/categories
```

---

## Шаг 6: Подключить Frontend

### Обновите API URL в frontend:

Файл: `frontend/js/config.js`

```javascript
// Было:
const API_BASE_URL = 'https://dongfeng-minitraktor.onrender.com/api';

// Стало:
const API_BASE_URL = 'https://dongfeng-minitraktor.vercel.app/api';
```

### Загрузите на Qwins.co через FileZilla:

1. Откройте FileZilla
2. Подключитесь к `94.181.229.248`
3. Загрузите обновленный `config.js` в `/www/js/`
4. Готово! ✅

---

## Шаг 7: Настройка автодеплоя

Vercel автоматически деплоит при каждом push:

```bash
# Внесите изменения
git add -A
git commit -m "Update backend"
git push origin main

# Vercel автоматически обновит сайт через 1-2 минуты
```

---

## Шаг 8: Настройка кастомного домена (опционально)

Если у вас есть домен (например, `dongfeng-minitraktor.ru`):

1. В Vercel Dashboard → **Settings → Domains**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкции Vercel
4. Готово! Сайт будет доступен по вашему домену

---

## 🔧 Переменные окружения Vercel

В Vercel Dashboard → **Settings → Environment Variables** добавьте:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `SUPABASE_URL` | `https://wbfhvcmvkyjsjvqkbxpz.supabase.co` | URL Supabase |
| `SUPABASE_KEY` | `eyJhbGciOi...` | Anon key Supabase |
| `NODE_ENV` | `production` | Режим работы |
| `FRONTEND_URL` | `http://web80563.hosted-by.qwins.co` | URL frontend для CORS |

---

## 🚀 Архитектура решения

```
┌──────────────────────────────────────────────────┐
│              ПОЛЬЗОВАТЕЛЬ                         │
└──────────────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│        Frontend (Qwins.co)                       │
│   http://web80563.hosted-by.qwins.co            │
│                                                  │
│   • HTML, CSS, JavaScript                       │
│   • Интерактивные анимации                      │
└──────────────────────────────────────────────────┘
                     │
                     │ API запросы
                     ▼
┌──────────────────────────────────────────────────┐
│        Backend (Vercel)                          │
│   https://dongfeng-minitraktor.vercel.app       │
│                                                  │
│   • Node.js Serverless Functions               │
│   • Fastify API                                │
└──────────────────────────────────────────────────┘
                     │
                     │ SQL запросы
                     ▼
┌──────────────────────────────────────────────────┐
│        Database (Supabase)                       │
│   https://wbfhvcmvkyjsjvqkbxpz.supabase.co      │
│                                                  │
│   • PostgreSQL база данных                      │
│   • Автоматические бэкапы                       │
│   • Бесплатно 500MB                             │
└──────────────────────────────────────────────────┘
```

---

## 💰 Стоимость

### Vercel Free Tier:
- ✅ **100% БЕСПЛАТНО**
- ✅ 100 GB bandwidth/месяц
- ✅ Неограниченные деплои
- ✅ Автоматический SSL
- ✅ Edge Network

### Supabase Free Tier:
- ✅ **Бесплатно навсегда**
- ✅ 500 MB базы данных
- ✅ 2 GB файлов
- ✅ 50 MB file uploads

### Qwins.co:
- ✅ Бесплатный хостинг для frontend

**💰 Итого: 0₽ / месяц**

---

## 🐛 Решение проблем

### Проблема: "Module not found"

**Решение:**
- Проверьте `package.json` в корне проекта
- Убедитесь, что все зависимости установлены

### Проблема: "CORS errors"

**Решение:**
1. Добавьте в переменные окружения Vercel:
   ```
   FRONTEND_URL=http://web80563.hosted-by.qwins.co
   ```

2. Проверьте CORS настройки в `backend/server.js`:
   ```javascript
   fastify.register(cors, {
     origin: process.env.FRONTEND_URL || '*',
     credentials: true
   });
   ```

### Проблема: "Supabase connection error"

**Решение:**
- Проверьте переменные `SUPABASE_URL` и `SUPABASE_KEY` в Vercel
- Убедитесь, что Supabase проект активен

---

## 📊 Мониторинг

Vercel предоставляет:
- ✅ Real-time логи функций
- ✅ Аналитику запросов
- ✅ Метрики производительности
- ✅ Rollback на предыдущую версию

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

1. **Backend на Vercel:** `https://dongfeng-minitraktor.vercel.app`
2. **Frontend на Qwins:** `http://web80563.hosted-by.qwins.co`
3. **База данных на Supabase:** PostgreSQL
4. **Автоматический деплой** при git push
5. **💰 Всё БЕСПЛАТНО!**

---

## 🔗 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Vercel Docs: https://vercel.com/docs
- Supabase Dashboard: https://app.supabase.com
- GitHub Repo: https://github.com/TARS911/dongfeng-minitraktor

---

## 🚀 Альтернатива: Railway

Если Vercel не подойдет, используйте Railway:
- См. инструкцию: `DEPLOY_RAILWAY.md`
- $5 кредитов/месяц

---

**Vercel + Supabase = Лучшее бесплатное решение для вашего проекта! 🎉**
