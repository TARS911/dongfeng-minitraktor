# 🚂 Деплой на Railway.app - Пошаговая инструкция

## Почему Railway?

✅ **Идеально для вашего проекта:**
- Node.js + Fastify работают из коробки
- SQLite база данных сохраняется
- $5 бесплатных кредитов каждый месяц
- Автоматический деплой из GitHub
- Простая настройка

---

## Шаг 1: Регистрация на Railway

1. Перейдите на: **https://railway.app**
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub (рекомендуется)
4. Подтвердите email

---

## Шаг 2: Подготовка репозитория

### Убедитесь, что код на GitHub:

```bash
cd /home/ibm/dongfeng-minitraktor
git add -A
git commit -m "🚂 Подготовка к деплою на Railway"
git push origin main
```

### Проверьте структуру:
```
dongfeng-minitraktor/
├── backend/
│   ├── server.js
│   ├── package.json
│   ├── database/
│   └── routes/
├── frontend/
├── railway.json       ← Создан
├── nixpacks.toml      ← Создан
└── README.md
```

---

## Шаг 3: Создание проекта на Railway

1. **В Dashboard Railway нажмите "New Project"**

2. **Выберите "Deploy from GitHub repo"**

3. **Выберите репозиторий:** `dongfeng-minitraktor`

4. **Railway автоматически:**
   - Обнаружит Node.js проект
   - Установит зависимости
   - Запустит build команды

---

## Шаг 4: Настройка переменных окружения

В Railway Dashboard → **Variables** добавьте:

```env
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
DB_PATH=./database/dongfeng.db
FRONTEND_URL=http://web80563.hosted-by.qwins.co
```

**Важно:** Railway автоматически предоставит переменную `PORT`, но мы добавляем свою для совместимости.

---

## Шаг 5: Настройка Build команды

Railway автоматически прочитает `nixpacks.toml`, но можно настроить вручную:

### В Settings → Build:
```bash
cd backend && npm install && npm run init-db && npm run seed-db
```

### В Settings → Deploy:
```bash
cd backend && npm start
```

---

## Шаг 6: Деплой!

1. Railway автоматически начнет деплой
2. Следите за логами в реальном времени
3. Дождитесь сообщения: **"Deployment completed"**

---

## Шаг 7: Получить публичный URL

1. В Railway Dashboard нажмите **"Settings"**
2. Найдите раздел **"Domains"**
3. Нажмите **"Generate Domain"**
4. Получите URL вида: `https://dongfeng-minitraktor-production.up.railway.app`

---

## Шаг 8: Тестирование API

```bash
# Проверка здоровья API
curl https://your-app.up.railway.app/api/health

# Получить товары
curl https://your-app.up.railway.app/api/products

# Получить категории
curl https://your-app.up.railway.app/api/categories
```

---

## Шаг 9: Подключить Frontend

### Обновите API URL в frontend:

Файл: `frontend/js/config.js`

```javascript
// Было:
const API_BASE_URL = 'https://dongfeng-minitraktor.onrender.com/api';

// Стало:
const API_BASE_URL = 'https://your-app.up.railway.app/api';
```

### Загрузите на FTP (qwins.co):

```bash
# Измените config.js локально, затем загрузите через FileZilla
```

---

## Шаг 10: Настройка автоматического деплоя

Railway автоматически деплоит при `git push`:

```bash
# Внесите изменения
git add -A
git commit -m "Update feature"
git push origin main

# Railway автоматически обновит сайт через 2-3 минуты
```

---

## 🔧 Настройка Persistent Storage (для SQLite)

Railway автоматически сохраняет файлы, но для гарантии:

1. В Railway Dashboard → **Settings**
2. Найдите **"Volumes"**
3. Добавьте volume:
   - Mount Path: `/app/backend/database`
   - Size: 1 GB (достаточно)

Это гарантирует, что база данных не потеряется при редеплое.

---

## 💰 Стоимость и лимиты

### Бесплатный план:
- **$5** кредитов каждый месяц
- Примерно **500 часов работы** маленького приложения
- Достаточно для тестирования и небольшого трафика

### Если кредиты закончатся:
- Добавьте карту (списание только за использование)
- Или переключитесь на другую платформу

---

## 🐛 Решение проблем

### Проблема: "Build failed"

**Решение:**
```bash
# Проверьте, что package.json в backend/ содержит скрипты:
"scripts": {
  "start": "node server.js",
  "init-db": "node database/init.js",
  "seed-db": "node database/seed.js"
}
```

### Проблема: "Database not found"

**Решение:**
- Убедитесь, что `DB_PATH=./database/dongfeng.db`
- Проверьте, что `npm run init-db` выполнился в логах

### Проблема: "CORS errors"

**Решение:**
- Добавьте ваш домен qwins.co в `FRONTEND_URL`
- Проверьте настройки CORS в `backend/server.js`

---

## 📊 Мониторинг

Railway предоставляет:
- ✅ Real-time логи
- ✅ Метрики использования CPU/RAM
- ✅ История деплоев
- ✅ Rollback на предыдущую версию

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

1. **Backend на Railway:** `https://your-app.up.railway.app`
2. **Frontend на Qwins:** `http://web80563.hosted-by.qwins.co`
3. **Автоматический деплой** при git push

---

## 🔗 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app
- GitHub Repo: https://github.com/yourusername/dongfeng-minitraktor

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных инициализирована

**Railway поддержка:** https://help.railway.app
